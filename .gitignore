node_modules/
.next/
out/
dist/
build/
.env*.local
.DS_Store
*.log
data/db.json
.claude/
# Standalone sub-app has been merged into app/launch/. Keep the source
# folder locally for reference but don't ship it.
Stansford CRM/
# Working note, not part of the deploy.
DEPLOYMENT.md
